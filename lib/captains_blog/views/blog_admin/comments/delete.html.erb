<h2>Are you sure you want to delete this comment by <%= @comment.commenter_display_name %>?</h2>
<p>This action cannot be undone. Press the "Delete comment" button to delete, or click "cancel" to cancel.</p>

<form action="<%= blog_root %>/admin/comments/<%= @comment.id %>/delete" method="post">
  <input type="hidden" name="_method" value="delete">
  <input type="submit" class="delete" value="Delete comment"> <a href="<%= CaptainsBlog.root %>/<%= @blog.slug %>/admin/posts/<%= @comment.post.id %>/comments" class="cancel" onclick="if ( $('#modalContainer').length == 1 ) { closeModal(); return false; }">cancel</a>
</form>